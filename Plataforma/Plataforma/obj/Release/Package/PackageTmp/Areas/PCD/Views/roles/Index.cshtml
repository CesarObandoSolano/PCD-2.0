@model IEnumerable<Plataforma.Areas.PCD.Models.role>

@{
    ViewBag.Title = "Indice";
}
<head>
    <script src="~/Scripts/jquery-1.10.2.js"></script>
    <script>
        function Buscar() {
            var nombre = document.getElementById('nombre').value;
            $("#tabla-roles").load("/PCD/roles/Index/?nombre=" + nombre + " #tabla-roles");
            return false;
        }
    </script>
</head>

<h2>Indice</h2>
<div class="col-lg-9">
    <p>
        @Html.ActionLink("Insertar un nuevo rol", "Create")
    </p>
    <div class="col-lg-9 barra-busqueda form-group">
        @Html.Label("Buscar:", htmlAttributes: new { @class = "col-md-2" })
        <input type="text" id="nombre" name="nombre" placeholder="Nombre del rol" class="form-control" onkeypress="Buscar()" />
    </div>
    <table class="table" id="tabla-roles">
        <tr>
            <th>
                Rol
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.rol)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.id }) |
                    @Html.ActionLink("Ver Detalles", "Details", new { id = item.id }) |
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.id })
                </td>
            </tr>
        }

    </table>
</div>
