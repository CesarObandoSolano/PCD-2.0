@model IEnumerable<Plataforma.Areas.PCD.Models.nivele>

@{
    ViewBag.Title = "Indice";
}
<head>
    <script src="~/Scripts/jquery-1.10.2.js"></script>
    <script>
        function Buscar() {
            var nombre = document.getElementById('nombre').value;
            $("#tabla-niveles").load("/PCD/niveles/Index/?nombre=" + nombre + " #tabla-niveles");
            return false;
        }
    </script>
</head>

<div class="col-lg-9">
    <h2>Niveles</h2>

    <p>
        @Html.ActionLink("Insertar un nivel", "Create")
    </p>
    <div class="col-lg-9 barra-busqueda form-group">
        @Html.Label("Buscar:", htmlAttributes: new { @class = "col-md-2" })
        <input type="text" id="nombre" name="nombre" placeholder="Descripción del nivel" class="form-control" onkeypress="Buscar()" />
    </div>
    <table class="table" id="tabla-niveles">
        <tr>
            <th>
                Nivel
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nivel)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.id }) |
                    @Html.ActionLink("Ver Detalles", "Details", new { id = item.id }) |
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.id })
                </td>
            </tr>
        }

    </table>
</div>
